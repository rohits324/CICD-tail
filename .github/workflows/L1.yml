name: Learning CICD

on:
 push:
  branches: ["main"]

job:
 build:
  runs-on: ubuntu-latest

  steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Print the CICD
      run: |
        echo "running Succesfully"

